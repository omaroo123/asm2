// Push x and y
push argument 0
push argument 1
add

// Store the result in argument[2]
dup  // Duplicate sum
pop argument 2

// Check for overflow: if sum < x, then overflow occurred
push argument 0  // Push x
lt  // Check if sum < x
if-goto OVERFLOW
goto NO_OVERFLOW

OVERFLOW:
  push constant 1
  pop argument 3  // argument[3] = 1 (overflow occurred)
  goto END

NO_OVERFLOW:
  push constant 0
  pop argument 3  // argument[3] = 0 (no overflow)

END:
